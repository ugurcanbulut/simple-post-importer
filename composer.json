{
    "name": "ugurcanbulut/simple-post-importer",
    "description": "Scan a remote WordPress site via its public REST API and import posts, images, terms, and authors.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=8.0"
    },
    "autoload": {
        "psr-4": {
            "SimplePostImporter\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    }
}
